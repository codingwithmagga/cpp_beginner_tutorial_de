#include <iostream>

int main()
{
    //   Gebe Hello World aus
    std::cout << "Hello World!" << std::endl;

    double a = 1.0;
    double b = 1.0;
    double c = 2.0;

    // Berechne das Produkt aus b und c und weiße es a zu
    a = b * c;

    // Berechne Norm des Vektors v
    /*
     * normberchnung des Vektors v
     */

    // Bubble Sort algorithmus, sortiert vektor v aufsteigend (<), bei gleichgroßen
    // Elementen (==) bleibt die Reihenfolge erhalten
    /*
     * code Zeilen zum Bubble Sort algorithmus
     */

    return 0;
}
